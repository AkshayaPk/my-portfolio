version: 0.1
phases:
  install:
    commands:
      - npm install
      - npm install -D webpack-cli
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - resources/Resume.pdf
    - styles/main.css
    - images/*
    - dist/bundle.js
