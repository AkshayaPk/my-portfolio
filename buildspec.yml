version: 0.1
phases:
  install:
    commands:
      - npm run webpack
  pre_build:
    commands:
      - npm install
      - npm test    
artifacts:
  files:
    - index.html
    - favicon.ico
    - resources/Resume.pdf
    - styles/main.css
    - images/*
    - dist/bundle.js
