version: 0.1
phases:
  pre-build:
    commands:
      - npm install
      - npm test
  install:
    commands:
      - npm run webpack
artifacts:
  files:
    - index.html
    - favicon.ico
    - resources/Resume.pdf
    - styles/main.css
    - images/*
    - dist/bundle.js
